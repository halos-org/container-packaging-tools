#!/bin/sh
set -e

case "$1" in
    remove|deconfigure)
        # Only interact with systemd if it's running (not in containers or minimal envs)
        if [ -d /run/systemd/system ]; then
            # Stop the service before removal
            systemctl stop simple-test-app-container.service || true
            systemctl disable simple-test-app-container.service || true
        fi
        ;;
esac

#DEBHELPER#

exit 0
